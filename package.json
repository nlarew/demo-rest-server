{
  "name": "promod-rest-server",
  "version": "1.0.0",
  "description": "Promod Realm REST server",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "serve": "node dist/server.js",
    "start": "npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vincent.mazot@mongodb.com",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongodb": "^3.5.9",
    "realm": "github:realm/realm-js#v10.0.0-beta.6"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "@types/express": "^4.17.6",
    "@types/mongodb": "^3.5.22",
    "@types/node": "^14.0.13",
    "tslint": "^6.1.2",
    "typescript": "^3.9.5"
  }
}
